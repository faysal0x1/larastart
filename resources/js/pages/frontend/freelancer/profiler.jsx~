import { Toaster } from 'sonner';
import Profile from '../../../components/frontend/employeeDashboard/Profile';
import AboutSection from '../../../components/frontend/employeeDashboard/AboutSection';
import VideoIntro from '../../../components/frontend/employeeDashboard/VideoIntro';
import EducationCertificates from '../../../components/frontend/employeeDashboard/EducationCertificates';
import SkillsBadges from '../../../components/frontend/employeeDashboard/SkillsBadges';
import EmployeeDashboardLayout from '../../../layouts/employeeDashboardLayout/EmployeeDashboardLayout';

const ProfileIndex = () => {
    return (
        <EmployeeDashboardLayout>
            <div className="bg-gray-50 min-h-screen">
                <Toaster
                    position="top-right"
                    richColors
                    closeButton
                    expand={true}
                    visibleToasts={3}
                />

                <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
                    {/* Mobile Order (stacked) */}
                    <div className="flex flex-col lg:hidden space-y-6">
                        <Profile />
                        <AboutSection />
                        <VideoIntro />
                        <EducationCertificates />
                        <SkillsBadges />
                    </div>

                    {/* Desktop Order (two columns) */}
                    <div className="hidden lg:flex lg:flex-col">
                        {/* Profile Header - Full width */}
                        <div className="mb-8">
                            <Profile />
                        </div>

                        {/* Main Content Area */}
                        <div className="flex flex-row gap-6">
                            {/* Left Column - Education */}
                            <div className="w-1/3">
                                <EducationCertificates />
                            </div>

                            {/* Right Column - Stacked components */}
                            <div className="w-2/3 space-y-6">
                                <AboutSection />
                                <VideoIntro />
                                <SkillsBadges />
                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </EmployeeDashboardLayout>
    );
};

export default ProfileIndex;