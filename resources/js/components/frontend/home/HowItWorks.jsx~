


const how-it-works = () => {

    const steps = [
        {
            id: 1,
            title: "Tell us what you need",
            description: "Post your micro-job with clear instructions and budget. It only takes a minute.",
        },
        {
            id: 2,
            title: "Receive offers from freelancers",
            description: "Get bids from qualified freelancers within minutes.",
        },
        {
            id: 3,
            title: "Choose the best freelancer",
            description: "Compare profiles, reviews, and proposals to find your perfect match.",
        },
    ];

    return (
        <div className="bg-white py-16 sm:py-24">
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div className="lg:text-center">
                    <h2 className="text-base font-semibold tracking-wide text-primary uppercase">How it works</h2>
                    <p className="mt-2 text-3xl font-extrabold leading-8 tracking-tight text-dark sm:text-4xl">
                        Get your micro-jobs done in 3 simple steps
                    </p>
                </div>

                <div className="mt-16">
                    <div className="grid grid-cols-1 gap-8 md:grid-cols-3">
                        {steps.map((step) => (
                            <div key={step.id} className="pt-6">
                                <div className="flow-root bg-gray-50 rounded-lg px-6 pb-8 h-full">
                                    <div className="-mt-6">
                                        <div className="flex items-center justify-center h-12 w-12 rounded-md bg-primary text-white">
                                            <span className="text-xl font-bold">{step.id}</span>
                                        </div>
                                        <h3 className="mt-4 text-lg font-medium text-dark">{step.title}</h3>
                                        <p className="mt-2 text-base text-gray-500">{step.description}</p>
                                    </div>
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </div>
        </div>
    );
};

export default how-it-works;